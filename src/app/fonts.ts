import {Outfit, Nunito} from 'next/font/google'


export const display_font = Outfit({
    subsets: ["latin"],
    display: "swap",
});

export const main_font = Nunito({
    subsets: ['latin'],
    display: "swap",
})